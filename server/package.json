{
  "name": "server",
  "version": "1.0.0",
  "license": "MIT",
  "main": "src/api-service/index.js",
  "type": "module",
  "scripts": {
    "start-node-server": "node src/api-service/index.js",
    "//": "using docker-compose will start connected setup of mongodb and node express app",
    "start": "docker-compose up --build",
    "watch": "nodemon src/api-service/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "helmet": "^4.6.0",
    "mongoose": "^5.12.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
